// WiFi credentials are shared across all environments that need them.
// Copy this file to firmware/include/wifi_config.h (already gitignored).
//
// Used by: imu_test (WiFi UDP mode) and mic_test (WiFi HTTP option)
//
#pragma once

#define WIFI_SSID      "your_network_name"
#define WIFI_PASS      "your_password"
#define IMU_UDP_PORT   4210
